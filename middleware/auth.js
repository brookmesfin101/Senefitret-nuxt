export default function ({app, route, redirect}) {    
    // if(String(route.fullPath).includes('admin')){    
    //     console.log('currentUser: ', app.$fire.auth.currentUser);
    //     if(!app.$fire.auth.currentUser) {                     
    //         return redirect('/auth/signin');                       
    //     }
    // }
}